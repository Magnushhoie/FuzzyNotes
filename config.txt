# Default folder for notes
notes_folder=$HOME/_FuzzyNotes

# Default note file used by fz and fze if no existing filename is provided
primary_note_file=$notes_folder/notes.txt

# Default text editor.
# Alternatives include vim, emacs, atom, sublime and open (opens with default text editor)
# Note: Editor open at searched line currently only supported with vim
EDITOR=open
